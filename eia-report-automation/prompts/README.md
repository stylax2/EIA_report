# 프롬프트 템플릿

`src/llm/prompts.py` 가 이 폴더의 파일을 읽어 사실 페이로드를 채운다.

## 작성 원칙

- 계산을 요구하지 않는다. 수치는 이미 확정된 상태로 전달된다.
- "주어진 수치만 사용한다", "목록에 없는 종을 추가하지 않는다" 같은
  제약을 명시한다.
- 문체 지시(서술식, 보고서 종결어미)를 포함한다.
- 치환 자리는 `{{FACTS}}` 형식으로 표시한다.

## 파일

| 파일 | 용도 |
|---|---|
| `survey_result_text.md` | 조사결과 설명 문장 |

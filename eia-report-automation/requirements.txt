# 지금 실행에 필요한 것만 둔다. 설치 시간과 실패 지점을 줄이기 위함이다.
# 아직 쓰지 않는 계층의 의존성은 아래에 주석으로 남겨 두고, 해당 계층을
# 구현할 때 주석을 해제한다.

# 데이터 로딩·분석 (src/data, src/analysis)
pandas>=2.2
openpyxl>=3.1

# HWPX(ZIP+XML) 템플릿 추출 (src/hwpx)
lxml>=5.0

# 테스트
pytest>=8.0

# ── 아직 쓰지 않음 ────────────────────────────────────────────────
# 웹페이지의 그래프는 인라인 SVG 로 그리므로 matplotlib 이 필요 없다.
# 평가서 삽입용 PNG 를 만들 때 해제한다. (src/charts)
# matplotlib>=3.8

# 설정 파일 로딩. config/*.yaml 을 실제로 읽기 시작할 때 해제한다.
# PyYAML>=6.0

# 로컬 LLM 런타임(Ollama) 연동. (src/llm)
# requests>=2.32

# 한글 문서 조판. Windows + 한글 프로그램이 있어야 한다. (src/hwpx)
# pyhwpx

# Windows 배치 파일은 CRLF 를 유지해야 한다. LF 로 체크아웃되면 cmd 가
# 레이블·goto 를 잘못 읽어 실행이 어긋날 수 있다.
*.bat text eol=crlf

# 셸 스크립트는 반대로 항상 LF 를 유지한다.
*.sh text eol=lf

# 엑셀·한글 문서는 바이너리다. 줄바꿈 변환이 파일을 깨뜨린다.
*.xlsx binary
*.hwpx binary
*.pdf binary
